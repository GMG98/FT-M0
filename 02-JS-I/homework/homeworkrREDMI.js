*
Variables = las variables son contenedores para almacenar datos "valores".

*Strings = es una clase para manipular cadenas de caracteres.

*Funciones(argumentos `return`) = es una funcion que sirve para devolverme "retorne"
un valor o caracteristica asignada.

*Declaraciones `if` = la funcion "if"
sirve para declarar un bloque de varias operaciones en funcion a una expresion.

*Valores booleanos(`true`, `false`) son aquellas variables que tienen un solo fin es verdadero o falso " si o no".